import 'package:flutter/material.dart';
import 'package:e_klinik_dr_h_m_chalim/widgets/admin/profile_admin.dart';

class ProfileAdmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: WidgetsProfileAdmin().buildProfile(context));
  }
}
